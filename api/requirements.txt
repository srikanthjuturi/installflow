aiohttp==3.11.11
alembic==1.19.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
# Pinned even though nothing imports it directly. azure-storage-blob and
# azure-communication-email both FLOAT their azure-core requirement, and pip
# resolves one version for both — so a resolver move could break blob uploads,
# and you would find that out from a container log after a deploy. The rest of
# this file is not a full freeze; this pin is a deliberate exception.
azure-core==1.41.0
# Transactional email — the temporary password a new account is created with.
# Pure Python (azure-core, isodate, typing-extensions are all py3-none-any), so
# Oryx has nothing to compile, unlike pywebpush below. Its async client rides on
# aiohttp, which is already listed above.
azure-communication-email==1.1.0
azure-storage-blob==12.24.0
bcrypt==5.0.0
certifi==2026.7.22
click==8.4.2
colorama==0.4.6
dnspython==2.8.0
email-validator==2.3.0
fastapi==0.141.1
greenlet==3.5.4
gunicorn==23.0.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
Mako==1.4.1
MarkupSafe==3.0.3
openpyxl==3.1.5
psycopg-binary==3.3.4
psycopg==3.3.4
pydantic-settings==2.14.2
pydantic==2.13.4
pydantic_core==2.46.4
# [crypto] pulls `cryptography`, which RS256 needs — Google's ID tokens are
# RS256, so verifying a Google sign-in fails without it. It is already installed
# as a transitive dependency of pywebpush below; this declares the INTENT, so
# dropping web push one day cannot silently break sign-in with a
# `NotImplementedError` nobody would connect to a push change.
PyJWT[crypto]==2.13.0
# Web push to the console. Pulls py-vapid, http-ece and cryptography; the last
# is a compiled wheel, and Oryx builds remotely on Azure — see api/AGENTS.md.
pywebpush==2.0.3
python-dotenv==1.2.2
python-multipart==0.0.20
PyYAML==6.0.3
SQLAlchemy==2.0.51
starlette==1.4.1
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.3
uvicorn-worker==0.4.0
uvicorn==0.52.1
watchfiles==1.2.0
websockets==17.0.1
