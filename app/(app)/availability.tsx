import { AvailabilityScreen } from '@/features/availability/screens/AvailabilityScreen';

/** Screen 3 — working days, daily job cap, time off. */
export default function AvailabilityRoute() {
  return <AvailabilityScreen />;
}
