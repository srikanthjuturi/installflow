import { AvatarOptionsSheet } from '@/features/profile/screens/AvatarOptionsSheet';

/** Photo source picker for the profile avatar. */
export default function AvatarOptionsRoute() {
  return <AvatarOptionsSheet />;
}
