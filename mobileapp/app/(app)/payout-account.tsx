import { PayoutAccountScreen } from '@/features/payout/screens/PayoutAccountScreen';

/** Profile → Payout account — the UPI id earnings are paid to. */
export default function PayoutAccountRoute() {
  return <PayoutAccountScreen />;
}
