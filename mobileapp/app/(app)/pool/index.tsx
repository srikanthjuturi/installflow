import { PoolScreen } from '@/features/jobs/screens/PoolScreen';

/** Screen 4 — jobs matching this technician's categories and pincodes. */
export default function PoolRoute() {
  return <PoolScreen />;
}
