import { RedeemConfirmSheet } from '@/features/redeem/screens/RedeemConfirmSheet';

/** The one deliberate tap between a balance and a request to be paid it. */
export default function RedeemConfirmRoute() {
  return <RedeemConfirmSheet />;
}
