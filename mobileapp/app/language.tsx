import { LanguageSheet } from '@/features/language/screens/LanguageSheet';

/** Choose the app's language — at the root, so it works before sign-in. */
export default function LanguageRoute() {
  return <LanguageSheet />;
}
