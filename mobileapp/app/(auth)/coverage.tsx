import { CoverageScreen } from '@/features/onboarding/screens/CoverageScreen';

/** R2 — categories + pincodes. Sets which jobs this technician is ever offered. */
export default function CoverageRoute() {
  return <CoverageScreen />;
}
