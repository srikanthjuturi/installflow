import { RegisterVerifyScreen } from '@/features/onboarding/screens/RegisterVerifyScreen';

/** The last step: prove the phone, then the whole registration commits at once. */
export default function RegisterVerifyRoute() {
  return <RegisterVerifyScreen />;
}
