import { RegisterProfileScreen } from '@/features/onboarding/screens/RegisterProfileScreen';

/** R1b — the technician's own name and photo. Invite path only. */
export default function RegisterProfileRoute() {
  return <RegisterProfileScreen />;
}
