import { PhotoViewerScreen } from '@/features/profile/screens/PhotoViewerScreen';

/** The technician's own profile photo, full screen — zoom, and remove. */
export default function ViewPhotoRoute() {
  return <PhotoViewerScreen />;
}
